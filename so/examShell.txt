#!/bin/bash
A=0
while read user; do
	var=$(last | grep "$user" | tail -3 | awk 'BEGIN{i=0} {i++} END{print i}')
	if [ -z $var ] ; then
		echo "user $user never logged in"
	else
		last | grep -s "$user" | tail -3
	fi
done